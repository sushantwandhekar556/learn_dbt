
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique             

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                # - not_null
                - relationships:
                    field: id
                    to: ref('my_first_dbt_model')
    - name: snowflake_customer_purches
      description: 'this is the total purches for each costomer in snowflake sample data'
      columns:
        - name: c_custkey
          tests:
            - unique
            - not_null
        - name: cummulitive_order_by_date
        - name: dates  